# Run basthon-notebook locally with Docker

A simple container combining nginx + basthon-notebook + pyodide.

Launch me with:

```
$ docker run -d -p 8888:80 nopid/basthon-docker
```

Enjoy a local basthon on http://localhost:8888/

